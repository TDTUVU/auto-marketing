{
  "name": "social-automation",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "build:all": "pnpm --filter @automation/core build && NODE_ENV=production pnpm --filter web build",
    "build:worker": "pnpm --filter @automation/core build",
    "start:web": "pnpm --filter web start",
    "start:worker": "tsx apps/web/worker.ts",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter web test",
    "test:post": "tsx scripts/test_post.ts",
    "test:metrics": "pnpm --filter @automation/core build && tsx --env-file=.env apps/web/test-metrics.ts",
    "worker": "pnpm --filter @automation/core build && tsx --env-file=.env apps/web/worker.ts"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "tsx": "^4.22.3",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
